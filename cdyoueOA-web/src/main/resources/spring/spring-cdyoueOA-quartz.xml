<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
                        http://www.springframework.org/schema/beans/spring-beans-3.2.xsd"
	default-autowire="byName">
	<!-- 定时任务实现 -->
	<!--<bean id="quartzTaskService"
    class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">  
    <property name="targetObject">  
    <bean class="com.youedata.cd.cdyoueOA.service.task.QuartzTaskService" />  
    </property>  
    <property name="targetMethod" value="doJob" />  
    <property name="concurrent" value="false" />&lt;!&ndash; 作业不并发调度  &ndash;&gt;
    </bean>  
    &lt;!&ndash; ======================== 调度触发器 ======================== &ndash;&gt;
	<bean id="CronTriggerBean" class="org.springframework.scheduling.quartz.CronTriggerBean">  
	    <property name="jobDetail" ref="quartzTaskService"></property>  
	    <property name="cronExpression" value="0 */1 * * * ?"></property>  
	</bean>  
  
	&lt;!&ndash; ======================== 调度工厂 ======================== &ndash;&gt;
	<bean id="SpringJobSchedulerFactoryBean" class="org.springframework.scheduling.quartz.SchedulerFactoryBean">  
	    <property name="triggers">  
	        <list>  
	            <ref bean="CronTriggerBean"/>  
	        </list>    
	    </property>
	    &lt;!&ndash; 禁用检查更新 &ndash;&gt;
	    <property name="quartzProperties"> 
			<props>
			 <prop key="org.quartz.scheduler.skipUpdateCheck">true</prop> 
			</props>
		</property>
	</bean>   -->
	
</beans>
